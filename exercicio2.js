let conta = Number(prompt("Adicione o valor da conta:"));
let clientes = Number(prompt("Quantos clientes pagaram a conta?"));

let total = conta / clientes;

alert("O total a ser pago vai ser: " + total);